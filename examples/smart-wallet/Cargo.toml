[package]
name = "crucible-example-smart-wallet"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
crucible = { path = "../../contracts/crucible" }
